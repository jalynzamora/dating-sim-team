/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package byui.cit260.control;

import byui.cit260.model.Game;
import byui.cit260.model.Map;

/**
 *
 * @author jalynzamora
 */
public class MapControl {

    public static Map createMap(Game game, int noOfRows, int noOfColumns){
    
        if(items == null || length< 1){
    return null;
    }
        if(noOfRows < 0 || noOfColumns < 0){
            return null;
        }
        

    /**
    * // create the map
     * object and assign values to it map = a new Map object call setter to
     * assign value to description in map call setter to assign value to
     * noOfRows in the map call setter to assign value to noOfColumns in map
     * call setter to save the map in game object // create a two-dimensional
     * array of locations and assign array to the map locations =
     * createLocations(noOfRows, noOfColumns) if (locations is null) then return
     * null endif call setter to save a locations array in the map object //
     * assign actors and items to locations success =
     * assignActorsToLocations(locations) if (success < 0) then return null
     * endif success = assignIemsToLocations(locations) if (success < 0) then
     * return null endif // Assign all other types objects to locations (e.g.,
     * questions, spells) *
     */
}
}
